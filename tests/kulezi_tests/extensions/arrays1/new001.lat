int main() {
    int a = 2, b = 3;
    int[] x = new int[a * b];

    int i = 0;
    while (i < a * b) {
        x[i] = 2 * i;
        printInt(i);
        printInt(x[i]);
        i++;
    }

    return 0;
}