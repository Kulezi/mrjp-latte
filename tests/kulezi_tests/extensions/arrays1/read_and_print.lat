int main() {
    int tests = readInt();
    while (tests > 0) {
        tests--;
        int n = readInt();
        int[] arr = new int[n];

        int i_init = 0;
        while (i_init < n) {
            arr[i_init] = readInt();
            i_init++;
        }

        int i = 0;
        while (i < n) {
            printInt(arr[i]);
            i++;
        }
        printString("");
    }

    return 0;
}