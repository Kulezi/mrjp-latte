int main() {
    boolean x = false;
    boolean y = true;
    if (x || (y && true)) {
        printInt(1);
        return 0;
    }

    if (x && (y || x && true) || false) {
        printInt(2);
        return 0;
    }

    printInt(3);
    return 0;
}